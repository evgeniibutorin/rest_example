#прописали URL нашей базы данных, сообщили логин и пароль для нее,
# прописали драйвер для PostgreSQL, обозначили, что будем использовать
# тип данных  PostgreSQL и прописали диалект для Hibernate.
spring.datasource.url=jdbc:postgresql://localhost:5432/shop
spring.datasource.username=root
spring.datasource.password=32167
# если указать строку ниже, то спринг возмет на себя создание структуры БД (БД создаем сами с принг структуру)
spring.jpa.generate-ddl=true

spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL10Dialect

#spring.datasource.initialization-mode=ALWAYS
#spring.datasource.schema=classpath*:database/initDB.sql
#spring.datasource.data=classpath*:database/populateDB.sql
# Если не закоментировать строку выше то
# при следующем запуске программы получим такую ошибку: org.postgresql.util.PSQLException:
# ОШИБКА: повторяющееся значение ключа нарушает ограничение уникальности "clients_pkey".
# Это происходит потому, что у нас уже заполнены в таблице поля с id 1 и 2 (еще при первом запуске).